const config = {
  size: 300,
  padding: 0,

  padAngle: 0.5,
  innerRadius: 70,
  labelRadius: (150 - 70) / 2 + 70 - 5,
  baseColor: '#3ea0da',
  emptyColor: '#e6e9ee',

  labels: {
    fontSize: 14,
    fontWeight: 600,
    fill: 'white'
  }
};

export default config;
